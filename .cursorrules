# User Rules - Выкуп авто

## ⚡ КРИТИЧЕСКИ ВАЖНЫЕ ПРАВИЛА РАБОТЫ AI

### 🔴 ОБЯЗАТЕЛЬНО ПЕРЕД ЗАВЕРШЕНИЕМ ЛЮБОЙ ЗАДАЧИ:

1. **ТЕСТИРОВАНИЕ НА LOCALHOST - ОБЯЗАТЕЛЬНО**
   - НИКОГДА не завершай задачу без проверки на localhost
   - Запускай `npm run dev` и проверяй, что все работает
   - Проверяй в браузере: http://localhost:3000
   - Если что-то не работает - ИСПРАВЛЯЙ сразу, не откладывай
   - Не говори "сделал, проверьте сами" - ПРОВЕРЬ САМ

2. **АНАЛИЗ КАЖДОГО ЭЛЕМЕНТА**
   - Каждый созданный/измененный компонент должен быть проанализирован на:
     * SEO оптимизацию (мета-теги, Schema.org, семантика)
     * Performance (Lighthouse метрики, bundle size, lazy loading)
     * Accessibility (ARIA, keyboard navigation, контраст)
     * Security (валидация, санитизация, rate limiting)
     * Code Quality (TypeScript strict, ESLint, типизация)
   - НЕ создавай "работающий" код - создавай "идеальный" код

3. **ПРОАКТИВНОЕ ПРЕДЛОЖЕНИЕ УЛУЧШЕНИЙ**
   - После каждого изменения анализируй:
     * Можно ли улучшить метрики?
     * Есть ли возможности для оптимизации?
     * Нужны ли дополнительные функции?
     * Соответствует ли элемент стандартам ТОП-1?
   - Предлагай конкретные улучшения, не жди запроса

4. **ДОВЕДЕНИЕ ДО СОВЕРШЕНСТВА ("ОБЛИЗЫВАНИЕ")**
   - Каждый элемент должен быть доведен до идеала:
     * Все edge cases обработаны
     * Все состояния (loading, error, success) реализованы
     * Все анимации плавные и осмысленные
     * Все тексты проверены на опечатки
     * Все изображения оптимизированы
     * Все формы валидированы
   - НЕ останавливайся на "работает" - доводи до "идеально"

5. **РАБОТА С МЕТРИКАМИ ТОП-1**
   - Каждое изменение должно улучшать метрики:
     * Lighthouse Score должен быть 100/100
     * Core Web Vitals в зеленой зоне
     * SEO Score максимальный
     * Accessibility Score 100%
   - Если метрика ухудшилась - ИСПРАВЛЯЙ немедленно
   - Измеряй метрики ДО и ПОСЛЕ изменений

6. **НЕ ЖДАТЬ - АНАЛИЗИРОВАТЬ И ПРЕДЛАГАТЬ**
   - Не жди указаний - анализируй проект и предлагай улучшения
   - Ищи узкие места в производительности
   - Ищи возможности для улучшения UX
   - Ищи возможности для улучшения SEO
   - Предлагай расширения функционала в рамках проекта

7. **ПРОВЕРКА РАБОТОСПОСОБНОСТИ**
   - После ЛЮБОГО изменения:
     * Проверь, что проект собирается: `npm run build`
     * Проверь, что нет ошибок TypeScript
     * Проверь, что нет ошибок ESLint
     * Проверь, что сервер запускается: `npm run dev`
     * Проверь в браузере, что все работает
   - Если что-то сломалось - ИСПРАВЛЯЙ сразу

8. **ДОКУМЕНТАЦИЯ И КОММЕНТАРИИ**
   - Все сложные решения должны быть задокументированы
   - JSDoc для всех публичных функций и компонентов
   - Комментарии для неочевидной бизнес-логики
   - Обновляй документацию при изменениях

### 🚫 ЗАПРЕЩЕНО:

- ❌ Завершать задачу без проверки на localhost
- ❌ Говорить "сделал, проверьте сами"
- ❌ Создавать код, который "работает, но не идеален"
- ❌ Игнорировать метрики производительности
- ❌ Пропускать валидацию и безопасность
- ❌ Использовать `any` в TypeScript
- ❌ Оставлять TODO комментарии без реализации
- ❌ Создавать компоненты без accessibility
- ❌ Использовать произвольные значения вместо Design System токенов

### ✅ ОБЯЗАТЕЛЬНО:

- ✅ Тестировать на localhost перед завершением
- ✅ Анализировать каждый элемент на все метрики
- ✅ Предлагать улучшения проактивно
- ✅ Доводить до совершенства каждый элемент
- ✅ Измерять метрики до и после изменений
- ✅ Исправлять проблемы немедленно
- ✅ Документировать сложные решения

---

## 🔄 ПРОЦЕСС РАБОТЫ НАД ЗАДАЧЕЙ

### Шаг 1: Анализ и планирование
1. Изучи существующий код и архитектуру проекта
2. Определи, какие компоненты/системы затронуты
3. Проверь метрики ДО изменений (если возможно)
4. Составь план с учетом всех аспектов ТОП-1

### Шаг 2: Реализация
1. Следуй всем правилам кодирования из этого файла
2. Используй только Design System токены
3. Типизируй все через TypeScript strict mode
4. Добавляй валидацию и безопасность
5. Реализуй все состояния (loading, error, success)
6. Добавляй ARIA атрибуты для accessibility

### Шаг 3: Тестирование на localhost
1. Запусти `npm run dev`
2. Открой http://localhost:3000 в браузере
3. Проверь все сценарии использования
4. Проверь на разных размерах экрана (responsive)
5. Проверь keyboard navigation
6. Проверь работу форм и валидацию
7. Проверь обработку ошибок

### Шаг 4: Проверка качества кода
1. Запусти `npm run lint` - должно быть 0 ошибок
2. Проверь TypeScript - должно быть 0 ошибок, 0 any
3. Проверь, что все импорты корректны
4. Проверь, что нет неиспользуемого кода

### Шаг 5: Проверка метрик
1. Запусти Lighthouse в браузере
2. Проверь Core Web Vitals
3. Проверь SEO Score
4. Проверь Accessibility Score
5. Если метрики ниже целевых - ИСПРАВЛЯЙ

### Шаг 6: Анализ и предложение улучшений
1. Проанализируй созданный/измененный элемент:
   - Можно ли улучшить производительность?
   - Можно ли улучшить UX?
   - Можно ли улучшить SEO?
   - Есть ли возможности для расширения функционала?
2. Предложи конкретные улучшения с обоснованием
3. Если улучшения критичны - реализуй их сразу

### Шаг 7: Финальная проверка
1. Убедись, что все работает на localhost
2. Убедись, что нет ошибок в консоли браузера
3. Убедись, что нет ошибок в терминале
4. Убедись, что метрики соответствуют целям ТОП-1
5. Только после этого сообщай о завершении

---

## 🎯 Философия проекта

**Главный принцип: ТОП-1 по всем метрикам**

Каждый элемент проекта должен быть лучшим в своем классе:
- SEO: позиция #1 по целевым запросам, 100% Schema.org покрытие
- Performance: Lighthouse Score 100/100, Core Web Vitals в зеленой зоне
- UX/UI: WCAG 2.1 AA, конверсия > 5%, мобильная адаптивность 100%
- Security: 0 уязвимостей, валидация 100% входных данных
- Code Quality: ESLint 0 ошибок, TypeScript strict mode, 0 any

**Принципы работы:**
- Непрерывное улучшение: каждое изменение должно улучшать метрики
- Data-driven подход: решения на основе данных и аналитики
- Best Practices First: следование индустриальным стандартам
- Проактивность: выявление и устранение проблем до их возникновения

---

## 🛠 Технологический стек

### Основные технологии
- **Next.js 14** (App Router) - фреймворк
- **TypeScript** (strict mode) - язык программирования
- **Tailwind CSS** - стилизация
- **React Hook Form** - работа с формами
- **Framer Motion** - анимации
- **React Icons** - иконки

### Правила использования
- ✅ Использовать App Router паттерны Next.js 14
- ✅ TypeScript strict mode, избегать `any` любой ценой
- ✅ Tailwind utilities вместо кастомных CSS классов
- ❌ Не использовать классовые компоненты (только функциональные)
- ❌ Не использовать inline styles (кроме динамических значений)

---

## 🏗 Архитектурные принципы

### Централизованная конфигурация
Все настройки приложения централизованы в `lib/config/`:

```typescript
// ✅ Правильно
import { APP_CONFIG } from '@/lib/config';
const phone = APP_CONFIG.PHONE_1;

// ❌ Неправильно
const phone = process.env.NEXT_PUBLIC_PHONE_1;
```

### Структура типов
Типы организованы по модулям в `lib/types/`:
- `types/forms.ts` - типы форм
- `types/api.ts` - типы API
- `types/domain.ts` - доменные типы
- `types/common.ts` - общие типы

```typescript
// ✅ Правильно
import type { FormData } from '@/lib/types/forms';

// ❌ Неправильно
type FormData = { ... } // Определение типов в компонентах
```

### Design System токены
Все дизайн-значения из `lib/design-system/`:

```typescript
// ✅ Правильно
import { SPACING, COLOR } from '@/lib/design-system';
const padding = SPACING.BASE; // '1rem'

// ❌ Неправильно
const padding = '16px'; // Произвольные значения
```

---

## 📝 Правила кодирования

### Именование

**Компоненты:**
- PascalCase для имен компонентов
- Описательные имена
- Суффикс `Section` для секций страниц

```typescript
// ✅ Правильно
export default function CarEvaluationForm() { }
export default function HeroSection() { }

// ❌ Неправильно
export default function Form1() { }
export default function Section() { }
```

**Переменные и функции:**
- camelCase для переменных и функций
- Описательные имена
- Boolean переменные начинаются с `is`, `has`, `should`

```typescript
// ✅ Правильно
const isLoading = true;
const hasError = false;
const shouldValidate = true;
const handleSubmit = () => { };

// ❌ Неправильно
const loading = true;
const error = false;
const validate = true;
```

**Props:**
- camelCase для имен props
- Boolean props начинаются с `is`, `has`, `should`

```typescript
// ✅ Правильно
interface ButtonProps {
  isLoading?: boolean;
  hasError?: boolean;
  onClick: () => void;
}

// ❌ Неправильно
interface ButtonProps {
  loading?: boolean;
  error?: boolean;
  click: () => void;
}
```

### Структура компонентов

Стандартный порядок в компонентах:

```typescript
'use client'; // Только если необходим client-side код

// 1. React и Next.js импорты
import React, { useState } from 'react';
import Link from 'next/link';

// 2. Сторонние библиотеки
import { motion } from 'framer-motion';
import { FiUser } from 'react-icons/fi';

// 3. Внутренние компоненты
import Button from '@/components/ui/Button';
import Card from '@/components/ui/Card';

// 4. Утилиты и типы
import { APP_CONFIG } from '@/lib/config';
import type { User } from '@/lib/types';

// 5. Стили (если есть)
import styles from './Component.module.css';

// Типы
interface ComponentProps {
  title: string;
  description?: string;
}

// Компонент
export default function Component({ title, description }: ComponentProps) {
  // Hooks
  const [state, setState] = useState();

  // Effects
  useEffect(() => { }, []);

  // Handlers
  const handleClick = () => { };

  // Render
  return <div>{/* JSX */}</div>;
}
```

### TypeScript правила

- ✅ Strict mode всегда включен
- ✅ Все props типизированы через `interface`
- ✅ Использовать `type` для union types и utility types
- ✅ Избегать `any` - использовать `unknown` если необходимо
- ✅ Опциональные props помечаются `?`

```typescript
// ✅ Правильно
interface FormProps {
  onSubmit: (data: FormData) => void;
  isDisabled?: boolean;
  className?: string;
}

// ❌ Неправильно
function Form(props: any) { } // any запрещен
```

### 'use client' директива

Использовать только когда необходим client-side код:
- useState, useEffect, useCallback, useMemo
- Обработчики событий (onClick, onChange)
- Framer Motion анимации
- Browser APIs (localStorage, window)

```typescript
// ✅ Правильно - нужен useState
'use client';
export default function InteractiveComponent() {
  const [state, setState] = useState();
}

// ✅ Правильно - Server Component
export default function StaticComponent() {
  return <div>Static content</div>;
}
```

---

## 🎨 Design System правила

### Использование токенов

Всегда используйте токены из Design System:

```tsx
// ✅ Правильно
<div className="p-4 bg-primary-600 text-white rounded-lg shadow-md">
  <h2 className="text-2xl font-bold">Заголовок</h2>
</div>

// ❌ Неправильно
<div className="p-[16px] bg-[#0284c7] text-white rounded-[8px]">
  <h2 className="text-[24px] font-[700]">Заголовок</h2>
</div>
```

### Spacing System

Базовая единица: 4px (0.25rem). Используйте стандартные значения:

```tsx
// ✅ Правильно
<div className="p-4 m-6 gap-2"> {/* 16px, 24px, 8px */}

// ❌ Неправильно
<div className="p-5 m-7 gap-3"> {/* Произвольные значения */}
```

### Color System

Используйте semantic colors для состояний:

```tsx
// ✅ Правильно
<div className="bg-green-50 text-green-800"> {/* Success */}
<div className="bg-red-50 text-red-800">     {/* Error */}
<div className="bg-yellow-50 text-yellow-800"> {/* Warning */}

// ❌ Неправильно
<div className="bg-[#f0fdf4] text-[#15803d]"> {/* Произвольные цвета */}
```

### Typography

Используйте стандартные размеры и веса:

```tsx
// ✅ Правильно
<h1 className="text-4xl font-extrabold"> {/* Page title */}
<h2 className="text-3xl font-bold">     {/* Section title */}
<p className="text-base font-normal">   {/* Body text */}

// ❌ Неправильно
<h1 className="text-[2.5rem] font-[900]"> {/* Произвольные значения */}
```

### Elevation System

Используйте систему elevation для теней:

```tsx
// ✅ Правильно
<div className="shadow-md"> {/* Elevation 2 */}
<div className="shadow-lg"> {/* Elevation 3 */}

// ❌ Неправильно
<div style={{ boxShadow: '0 5px 10px rgba(0,0,0,0.1)' }}>
```

### Tailwind utilities

Предпочитайте Tailwind классы кастомным CSS:

```tsx
// ✅ Правильно
<div className="p-4 rounded-lg shadow-md bg-primary-600">

// ❌ Неправильно
<div className="custom-padding custom-radius custom-shadow">
```

---

## 🔍 SEO и Performance требования

### Мета-теги

Все страницы должны иметь полный набор мета-тегов:

```typescript
// ✅ Правильно
export const metadata = {
  title: 'Заголовок страницы | Выкуп авто',
  description: 'Описание страницы для SEO',
  keywords: 'ключевые, слова',
  openGraph: {
    title: 'Заголовок',
    description: 'Описание',
    images: ['/og-image.jpg'],
  },
};
```

### Schema.org разметка

Добавляйте Schema.org разметку где применимо:

```typescript
// ✅ Правильно
const schema = {
  '@context': 'https://schema.org',
  '@type': 'Organization',
  name: 'Выкуп авто',
  // ...
};
```

### Оптимизация изображений

- Использовать Next.js Image компонент
- Форматы: WebP, AVIF
- Lazy loading для изображений ниже fold
- Оптимальные размеры для разных устройств

```tsx
// ✅ Правильно
<Image
  src="/image.jpg"
  alt="Описание"
  width={800}
  height={600}
  loading="lazy"
  placeholder="blur"
/>

// ❌ Неправильно
<img src="/image.jpg" alt="Описание" />
```

### Performance метрики

Целевые показатели:
- Lighthouse Score: 100/100
- First Contentful Paint (FCP): < 1.8s
- Largest Contentful Paint (LCP): < 2.5s
- Time to Interactive (TTI): < 3.8s
- Cumulative Layout Shift (CLS): < 0.1
- Total Blocking Time (TBT): < 200ms

---

## 🔒 Security требования

### Валидация входных данных

Все пользовательские данные должны быть валидированы:

```typescript
// ✅ Правильно
import { validatePhone, validateName } from '@/lib/validation';

const phone = validatePhone(formData.phone);
if (!phone.isValid) {
  return { error: phone.error };
}

// ❌ Неправильно
const phone = formData.phone; // Без валидации
```

### Санитизация

Санитизируйте весь пользовательский контент:

```typescript
// ✅ Правильно
import { sanitize } from '@/lib/sanitization';

const cleanContent = sanitize(userInput);

// ❌ Неправильно
const content = userInput; // Без санитизации
```

### Rate Limiting

Все API endpoints должны иметь rate limiting:

```typescript
// ✅ Правильно
import { rateLimit } from '@/lib/rateLimit';

export const POST = rateLimit(
  async (req: Request) => {
    // Handler
  },
  { windowMs: 60000, max: 5 }
);
```

### Security Headers

Security headers настроены в `next.config.js` и `middleware.ts`. Не изменять без необходимости.

---

## ✨ Code Quality

### ESLint

- 0 ошибок, 0 предупреждений
- Запускать `npm run lint` перед коммитом

### TypeScript

- Strict mode всегда включен
- 0 использований `any`
- Все функции и компоненты типизированы

### Комментарии

- JSDoc для всех публичных компонентов и функций
- Комментарии для сложной бизнес-логики
- Не комментировать очевидный код

```typescript
// ✅ Правильно
/**
 * CarEvaluationForm - форма оценки автомобиля
 *
 * @param onSubmit - Callback при успешной отправке формы
 * @param initialData - Начальные данные формы (опционально)
 */
export default function CarEvaluationForm({ onSubmit, initialData }: Props) {
  // Сложная логика расчета цены
  const price = useMemo(() => {
    // ... сложные вычисления
  }, [dependencies]);
}

// ❌ Неправильно
// Функция для формы
function Form() { }
```

### Тестируемый код

- Изолированная логика
- Четкие props и зависимости
- Предсказуемое поведение
- Тесты для критических путей

---

## ♿ Accessibility

### WCAG 2.1 AA соответствие

- Минимальный контраст текста: 4.5:1 для обычного текста, 3:1 для крупного
- Минимальный размер кликабельных элементов: 44x44px
- Правильные ARIA атрибуты
- Keyboard navigation для всех интерактивных элементов

### ARIA атрибуты

```tsx
// ✅ Правильно
<button
  aria-label="Закрыть модальное окно"
  aria-expanded={isOpen}
  onClick={handleClose}
>
  <FiX aria-hidden="true" />
</button>

<input
  aria-invalid={hasError}
  aria-describedby={hasError ? 'error-message' : undefined}
/>

// ❌ Неправильно
<button onClick={handleClose}>
  <FiX />
</button>
```

### Keyboard Navigation

Все интерактивные элементы должны быть доступны с клавиатуры:

```tsx
// ✅ Правильно
<button
  onClick={handleClick}
  onKeyDown={(e) => {
    if (e.key === 'Enter' || e.key === ' ') {
      handleClick();
    }
  }}
>
  Click me
</button>
```

---

## 🚀 Best Practices

### React оптимизации

**React.memo для тяжелых компонентов:**

```typescript
// ✅ Правильно
export default React.memo(function ExpensiveComponent({ data }) {
  // Тяжелые вычисления
});

// ❌ Неправильно
export default function ExpensiveComponent({ data }) {
  // Рендерится при каждом изменении родителя
}
```

**useMemo для вычислений:**

```typescript
// ✅ Правильно
const expensiveValue = useMemo(() => {
  return computeExpensiveValue(data);
}, [data]);

// ❌ Неправильно
const expensiveValue = computeExpensiveValue(data); // Вычисляется каждый рендер
```

**useCallback для handlers:**

```typescript
// ✅ Правильно
const handleClick = useCallback(() => {
  // Handler logic
}, [dependencies]);

// ❌ Неправильно
const handleClick = () => { }; // Создается новый function каждый рендер
```

### Error Boundaries

Используйте Error Boundaries для критических компонентов:

```tsx
// ✅ Правильно
<ErrorBoundary fallback={<ErrorFallback />}>
  <CriticalComponent />
</ErrorBoundary>
```

### Bundle Size оптимизация

- Code splitting для больших компонентов
- Lazy loading для не критичных компонентов
- Динамические импорты где возможно

```typescript
// ✅ Правильно
const HeavyComponent = dynamic(() => import('./HeavyComponent'), {
  loading: () => <LoadingSpinner />,
});

// ❌ Неправильно
import HeavyComponent from './HeavyComponent'; // Загружается сразу
```

### Условный рендеринг

```tsx
// ✅ Правильно
{isVisible && <Component />}
{isLoading ? <Spinner /> : <Content />}

// ❌ Неправильно
{isVisible ? <Component /> : null} // Избыточно
```

### Списки и ключи

```tsx
// ✅ Правильно
{items.map((item) => (
  <Item key={item.id} {...item} />
))}

// ❌ Неправильно
{items.map((item, index) => (
  <Item key={index} {...item} /> // index как key
))}
```

---

## 📚 Дополнительные ресурсы

- [PROJECT-VISION.md](./PROJECT-VISION.md) - Философия проекта
- [DESIGN-SYSTEM.md](./DESIGN-SYSTEM.md) - Design System документация
- [lib/component-system/rules.md](./lib/component-system/rules.md) - Правила компонентов
- [SEO-CHECKLIST.md](./SEO-CHECKLIST.md) - SEO чеклист
- [PERFORMANCE-CHECKLIST.md](./PERFORMANCE-CHECKLIST.md) - Performance чеклист

---

## ✅ Чеклист перед коммитом

Перед каждым коммитом проверьте:

- [ ] ESLint: 0 ошибок, 0 предупреждений
- [ ] TypeScript: 0 ошибок, 0 any
- [ ] Все props типизированы
- [ ] Используются токены Design System
- [ ] Добавлены мета-теги (для новых страниц)
- [ ] Schema.org разметка (где применимо)
- [ ] Валидация входных данных
- [ ] ARIA атрибуты для интерактивных элементов
- [ ] Оптимизированы изображения
- [ ] Тесты проходят (если есть)
- [ ] Документация обновлена (если необходимо)

---

**Помни: Цель - ТОП-1. Всегда. По всем метрикам. Без компромиссов.**
