# 🚀 Быстрый старт Lighthouse тестирования

## ⚠️ Важно: Проблема с Windows

На Windows может возникать ошибка `EPERM` при завершении Lighthouse. Это известная проблема, но **отчеты все равно создаются успешно**. Ошибка появляется только при очистке временных файлов.

## ✅ Решение

### Вариант 1: Использовать Chrome DevTools (Рекомендуется)

1. Откройте страницу в Chrome: http://localhost:3000
2. Нажмите `F12` для открытия DevTools
3. Перейдите на вкладку **Lighthouse**
4. Выберите категории: Performance, Accessibility, Best Practices, SEO
5. Нажмите **"Analyze page load"**

### Вариант 2: Использовать CLI с обработкой ошибок

Скрипт `scripts/lighthouse-test.js` обрабатывает ошибки и создает отчеты, даже если появляется ошибка `EPERM`.

```bash
node scripts/lighthouse-test.js
```

Отчеты будут созданы в папке `lighthouse-reports/`, несмотря на ошибку в конце.

### Вариант 3: Тестировать по одной странице

```bash
# Создайте директорию для отчетов
mkdir lighthouse-reports

# Протестируйте главную страницу
lighthouse http://localhost:3000/ --output=html --output-path=./lighthouse-reports/home.html --chrome-flags="--headless" --only-categories=performance,accessibility,best-practices,seo

# Откройте отчет
start lighthouse-reports/home.html
```

---

## 📊 Проверка результатов

После запуска Lighthouse проверьте:

1. **HTML отчеты** в папке `lighthouse-reports/`
2. **Метрики:**
   - Performance: должен быть 90+/100 (цель: 100/100)
   - Accessibility: должен быть 100/100
   - Best Practices: должен быть 100/100
   - SEO: должен быть 100/100

3. **Core Web Vitals:**
   - FCP < 1.8s
   - LCP < 2.5s
   - TTI < 3.8s
   - CLS < 0.1
   - TBT < 200ms

---

## 🎯 Рекомендация

**Используйте Chrome DevTools** для тестирования - это самый надежный способ на Windows, и вы сразу увидите результаты в браузере.

---

**Помни:** Ошибка `EPERM` не критична - отчеты создаются успешно!
